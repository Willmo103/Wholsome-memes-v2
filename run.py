from app import app

if __name__ == "__main__":
        app.run(
            debug=True
        ) # this is only for the development environment

        # app.run(
        #     host="127.0.0.1", port=
        # ) # this is only for the development environment
